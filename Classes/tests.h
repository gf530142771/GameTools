#ifndef _TESTS_H_
#define _TESTS_H_
#include "FlipCardSpriteLayer.h"
enum
{
    TEST_FLIP_CARD = 0,
	// last one
	TESTS_COUNT,
};

const std::string g_aTestNames[TESTS_COUNT] = {
    "flipCard",
};

#endif
